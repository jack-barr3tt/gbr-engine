apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - queuer/deployment.yaml

secretGenerator:
  - name: secrets
    envs:
      - ../../.env

generatorOptions:
  disableNameSuffixHash: true
